interface:
  display_name: "Design Pages"
  short_description: "Redesign application pages without breaking behavior"
  default_prompt: "Use $design-pages to analyze and modernize this application page while preserving all existing functionality."
